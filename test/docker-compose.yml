version: '3.3'

services:
    node-web-app:
        build: .
        image: kjax/node-web-app
        command: npm run start
        volumes:
            - ./public:/usr/src/public:Z
        ports:
            - "49160:8080"