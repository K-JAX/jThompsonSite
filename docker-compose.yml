version: '3.3'

services:
    node-web-app:
        build: ./frontend
        image: kjax/node-web-app
        command: gatsby develop -H 0.0.0.0
        volumes:
            - ./frontend/src:/home/node/app/src:Z
        ports:
            - "3000:8000"
        restart: always
    db-headless:
        container_name: db-headless
        user: "${USERID}:${GROUPID}"
        environment:
            - MYSQL_RANDOM_ROOT_PASSWORD=yes
            - MYSQL_DATABASE=${WORDPRESS_DB_NAME}
            - MYSQL_PASSWORD=${WORDPRESS_DB_PASSWORD}
            - MYSQL_USER=${WORDPRESS_DB_USER}
        image: mariadb
        volumes:
            - ./data:/var/lib/mysql:Z
        ports:
            - '3307:3306'
        restart: always
    wp-headless:
        build: ./wordpress
        command: bash -c 'bash install_wordpress.sh'
        user: root
        env_file: ./.env
        depends_on:
            - db-headless
        env_file: ./.env
        volumes:
            - ./wordpress:/var/www/html:z
            - ./docker/plugins:/var/www/plugins 
        ports:
        - '8080:8080'
        restart: always